## $Id$
##
## Top level automake file for gnucap
ACLOCAL_AMFLAGS = -Im4

SUBDIRS=	include lib modelgen main apps tests

EXTRA_DIST=	autogen.sh Makefile.template configure.static

dist_bin_SCRIPTS = gnucap-conf
